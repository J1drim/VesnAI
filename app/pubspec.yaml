name: vesnai_app
description: "VesnAI second-brain client (mobile + desktop). MIT-licensed; see the repository LICENSE."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.12.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_riverpod: ^2.5.1
  http: ^1.2.0
  # Native URLSession on Apple platforms (Cronet omitted — AGP 9 namespace clash).
  cupertino_http: ^3.0.2
  okf_dart:
    path: ../packages/okf_dart

  # Phase 1: pairing + secure connection persistence + QR scan
  flutter_secure_storage: ^10.3.1
  mobile_scanner: ^7.0.0

  # Phase 2: durable local note mirror (Drift/SQLite)
  drift: ^2.20.0
  sqlite3_flutter_libs: ^0.5.24
  path: ^1.9.0
  path_provider: ^2.1.4

  # Phase 3: capture attachments
  image_picker: ^1.1.2
  file_picker: ^12.0.0-beta.7

  # Phase 4: voice + backup export/share
  # On-device speech-to-text (iOS Speech / Android SpeechRecognizer). Used in
  # chat so the transcript appears instantly; only text is sent to the server.
  speech_to_text: ^7.4.0
  just_audio: ^0.9.40
  share_plus: ^13.2.1

  # Phase 5: LAN server discovery (mDNS)
  nsd: ^5.0.1

  # Phase 7: i18n + completion notifications
  flutter_localizations:
    sdk: flutter
  intl: any
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  flutter_local_notifications: ^22.0.1
  flutter_force_directed_graph: ^1.0.8
  shared_preferences: ^2.5.3
  flutter_markdown: ^0.7.7+1
  flutter_quill: ^11.5.1
  markdown: ^7.3.0
  markdown_quill: ^4.3.0
  url_launcher: ^6.3.1
  timezone: ^0.11.0
  flutter_timezone: ^5.1.0
  workmanager: ^0.9.0+3
dev_dependencies:
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter
  mocktail: ^1.0.3

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  drift_dev: ^2.20.0
  build_runner: ^2.4.12
  flutter_launcher_icons: ^0.14.4

# package_info_plus 10.2 adds built-in Kotlin (fixes KGP warning).
# Beta overrides bridge file_picker/quill to win32 6 until stable releases catch up.
dependency_overrides:
  package_info_plus: ^10.2.0
  quill_native_bridge_windows: 0.1.0-beta.1

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Generate AppLocalizations from lib/l10n/*.arb (see l10n.yaml).
  generate: true

  # assets/certs/ optionally holds the dev mkcert root CA (gitignored; copied
  # by ./scripts/vesnai.sh setup-https). Declared as a directory so builds
  # succeed when it is absent — see lib/data/http_client_factory.dart.
  assets:
    - assets/certs/
    - assets/branding/vesnai_logo.png
    - assets/branding/vesnai_icon.png

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

flutter_launcher_icons:
  android: true
  ios: true
  macos:
    generate: true
  image_path: assets/branding/vesnai_icon.png
  adaptive_icon_background: "#E2E6CE"
  adaptive_icon_foreground: assets/branding/vesnai_icon.png
  remove_alpha_ios: true
